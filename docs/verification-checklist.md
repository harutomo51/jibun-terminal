# Verification Checklist

Windows 11上で `npm run dev` を実行し、アプリ内のPowerShellプロンプトで確認します。

- [ ] `dir` が動く
- [ ] `Get-ChildItem` が動く
- [ ] `cd` が動く
- [ ] `git --version` が動く
- [ ] `npm --version` が動く
- [ ] 日本語入力ができる
- [ ] コピー・貼り付けができる
- [ ] ウィンドウリサイズで表示が崩れない
- [ ] 背景プリセット切替ができる
- [ ] 任意のアプリ背景色を選択できる
- [ ] 任意のターミナル背景色を選択できる
- [ ] ターミナル透明度をスライダーで連続調整できる
- [ ] サイドパネルに現在のディレクトリ内のフォルダとファイルが表示される
- [ ] サイドパネルの更新ボタンでファイル一覧を再読み込みできる
- [ ] `Ctrl+C` が効く
- [ ] 対話CLIが起動できる
- [ ] 終了時にPTYプロセスが残らない

## Notes

- PowerShell 7がある環境では `pwsh.exe` が優先されます。
- PowerShell 7がない環境では `powershell.exe` を使います。
- どちらも見つからない場合は画面上にエラーが表示されます。
- ファイルツリーは閲覧専用です。
