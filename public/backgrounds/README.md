# Backgrounds

Place optional background images for the terminal UI in this directory.

The CSS includes a sample reference to:

```text
public/backgrounds/sample-terminal-bg.jpg
```

If the image does not exist, the UI falls back to layered CSS gradients.
